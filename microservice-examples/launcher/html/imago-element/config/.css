.container {
    position: absolute;
    font-family: 'Pontano Sans', sans-serif;
    color: #d8d8d8;
    background-color: #2b2b2bce;
    max-width: 90%;
    bottom: 0;
    margin: 1em;
    padding: 1em;
    opacity: 0.2;
    transition: opacity 0.15s ease-out;
}

.container:hover {
    opacity: 1;
}

.header {
    flex: 0;
    display: none;
    width: 100%;
    pointer-events: none;
    margin: 0.5em 0;
}

.header h4 {
    padding: 0;
    margin: 0 .5em;
}

.config-items {
    height: 100%;
    width: 100%;
    overflow: auto;
    display: grid;
    grid-template-rows: 1em auto auto 1em;
    grid-auto-columns: auto;
}

.config-item {
    width: 100%;
    align-self: center;
    display: flex;
    justify-content: space-between;
}

.nested {
    display: grid;
    grid-auto-rows: auto;
    grid-template-columns: 1em auto;
}

.right-col {
    grid-column-end: -1;
}

.hidden {
    display: none !important;
}

.btn-spawn-point {
    display: flex;
    flex-wrap: wrap;
}

.btn-spawn-point .btn-container:first-child {
    margin-left: 0;
}

.btn-spawn-point .btn-container {
    margin-left: 0.5em;
}